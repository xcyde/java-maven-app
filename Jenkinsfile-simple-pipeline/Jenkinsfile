#!/usr/bin/env groovy

def gv

pipeline {
    agent any
    tools {
        maven 'Maven'
    }
    stages {
        stage("init") {
            steps {
                script {
                    checkout([
                        $class: 'GitSCM',
                        branches: [[name: '*/jenkins-jobs']],
                        userRemoteConfigs: [[url: 'https://github.com/xcyde/java-maven-app.git']],
                        extensions: [
                            [$class: 'CloneOption', shallow: true, noTags: false, reference: '', timeout: 1500]
                        ]
                    ])
                    gv = load "Jenkinsfile-simple-pipeline/script.groovy"
                }
            }
        }
        stage("build jar") {
            steps {
                script {
                    gv.buildJar()
                }
            }
        }
        stage("build image") {
            steps {
                script {
                    gv.buildImage()
                }
            }
        }
        stage("deploy") {
            steps {
                script {
                    gv.deployApp()
                }
            }
        }
    }
}
